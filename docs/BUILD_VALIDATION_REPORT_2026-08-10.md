# Build validation report — 2026-08-10

## Passed in this workspace

- Repository integrity, imported-source hashes and seven portable canonical ZIPs.
- Twelve embedded-payload byte extractions and Python compilation.
- Reproduction DAG validation and source existence.
- Dataset/license registry validation; U9 absent from all default profiles.
- Non-destructive adaptation and compilation of all 46 `full-claim` Python/notebook
  sources.
- U2 authoritative checkpoint/cache inventory and 38-target frozen metric reference.
- Five reproduction-runner unit tests.
- Exact-ID Drive cleanup proposal generation; no mutation performed.

## Previously validated package component

The current MATLAB figure package and PNG/TIFF visual QA are recorded in
`docs/LOCAL_ACCEPTANCE_FIXES_2026-08-10.md` and
`docs/R4_2_COMPATIBILITY_PDF_VALIDATION.md`.

## Not claimed as executed here

- The public network smoke download was not completed in this build workspace because
  an external-action execution limit blocked the command before it ran.
- The multi-day raw-data/GPU `full-claim` replay was not executed here.
- MATLAB U8 and replay-derived figure rendering were not rerun in this Linux build
  workspace.
- No Google Drive file was deleted, moved or overwritten.
- No GitHub repository was created or pushed; the configured remote was not accessible
  through the connected GitHub installation at build time.

Those items must remain reported as pending until their own run ledgers and remote
commit identifiers exist. Static readiness is not evidence of a successful full
scientific replay.
