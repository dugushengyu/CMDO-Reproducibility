# Reconstructed Figure-5 stress source

This directory is generated by `RUN_FIGURE5_STRESS_RECONSTRUCTED.m` and is deliberately separate from historical frozen source-data directories. Outputs here are post-completion synthetic comparator results. They do not silently replace the provenance of the original 2026-08-31 stress run or any prospective/locked empirical stage.
