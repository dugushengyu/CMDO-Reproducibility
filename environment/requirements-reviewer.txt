# Legacy/maintainer convenience environment.
#
# The current submission-v2 reviewer path does NOT install or require this
# file: RUN_REVIEWER.py uses the Python standard library plus MATLAB and reads
# tracked frozen source data directly.
#
# These pins are retained only for older maintainer utilities that may still
# use the former smoke/frozen reviewer profiles.
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
matplotlib==3.9.2
Pillow==11.0.0
joblib==1.4.2
